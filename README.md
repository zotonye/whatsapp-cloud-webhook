WhatsApp Cloud API – Minimal Webhook

Environment Variables required:
- PORT (Render sets this automatically)
- VERIFY_TOKEN (any secret text you choose)

Endpoints:
GET /webhook  -> verification
POST /webhook -> receive messages